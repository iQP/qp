{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FormCategory creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('f_formcategory') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
